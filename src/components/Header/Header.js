import React from 'react'
import { IndexLink, Link } from 'react-router'
import AppBar from 'material-ui/AppBar';

import './Header.scss'

export const Header = () => (
  <AppBar title="Feedback configuration">
  </AppBar>
)

export default Header
